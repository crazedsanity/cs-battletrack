<?php
$xmlData = array(
	'result'		=> "Invalid request",
	'numUpdates'	=> 0
);
//TODO: implement something here to check for external updates/notifications.
if(isset($_GET) && is_array($_GET) && count($_GET)) {
}

?>
